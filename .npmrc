engine-strict=true
# This repo is pnpm-only (see the "preinstall" guard + "packageManager"). Stop
# npm from writing a stray package-lock.json if someone runs `npm install` anyway.
package-lock=false
