{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Watch coverage on open",
            "command": "${command:coverage-gutters.watchCoverageAndVisibleEditors}",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
